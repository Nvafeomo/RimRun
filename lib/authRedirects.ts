/**
 * Must match Supabase → Authentication → URL Configuration → Redirect URLs
 * and match the app scheme in app.json (`scheme`).
 */
export const PASSWORD_RESET_REDIRECT_URL = 'rimrun://reset-password';
